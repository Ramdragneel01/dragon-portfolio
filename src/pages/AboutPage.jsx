// Generated by GitHub Copilot
import { Link } from "react-router-dom";
import About from "../components/About";

/**
 * Render the dedicated about page.
 *
 * @param {{config: Record<string, any>, profile: Record<string, any>}} props Component props.
 * @returns {JSX.Element} About page section.
 */
function AboutPage({ config, profile }) {
    return (
        <>
            <div className="pt-4">
                <Link to="/" className="glass-chip inline-flex rounded-full px-4 py-2 text-xs font-semibold uppercase tracking-[0.14em] text-[var(--text-primary)]">
                    Back to Home
                </Link>
            </div>
            <About config={config} profile={profile} />
        </>
    );
}

export default AboutPage;
